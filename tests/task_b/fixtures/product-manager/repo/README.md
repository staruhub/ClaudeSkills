# Skill Trust Card

This fixture represents a curated index, not a package installer. A resource
entry currently shows a title, URL, and short description. The proposed change
must help readers judge evidence without claiming that an entry is safe.

The index never executes a listed skill and does not collect account
credentials.

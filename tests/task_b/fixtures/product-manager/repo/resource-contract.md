# Current resource contract

Each resource has three required fields:

- `title`
- `url`
- `description`

Rendering is static. There is no evaluation status, tested commit, test date, or
limitations field. Unknown fields are ignored by the current page.
